# The agent's URL. A base URL is fine — /chat is appended when absent.
VITE_AGENT_URL=http://localhost:8000

# none | oidc
VITE_AUTH_MODE=none

# Required when VITE_AUTH_MODE=oidc. Register http://localhost:5173/ as the
# callback URL with your provider.
# VITE_OIDC_ISSUER=https://thunder.amp.localhost:8080
# VITE_OIDC_CLIENT_ID=
# VITE_OIDC_SCOPES=openid profile email

# VITE_COMPANY_NAME=O2 Insurance
